# dependencies
node_modules/
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# typescript
*.tsbuildinfo
next-env.d.ts

# test data — see fixtures/README.md
# Founder's real Duke snapshots, syllabi, and schedule stay out of git until
# scrubbed. Spec-derived fixtures (hand-authored from published API docs, no
# real student data) are committed. `/fixtures/*` rather than `/fixtures/`
# because git cannot re-include a path whose parent directory is excluded.
/fixtures/*
!/fixtures/.gitkeep
!/fixtures/README.md
!/fixtures/canvas/
!/fixtures/ical/
!/fixtures/changes/
# Extraction eval fixtures: public course pages (MIT OCW / Stanford / CMU, cited
# per fixture) plus one synthetic weekly schedule. No student data, and core.md's
# Definition of done requires them checked in and running in CI.
!/fixtures/extraction/
!.env.example

.vercel

# eve (agent runtimes): build/discovery artifacts, dev caches, auto-installed optional peers
agent/**/.eve/
agent/**/node_modules/
.eve/
.output/

# Spike A scratch (jsonl logs, generated attachments)
agent/voice/.spike/

# local dev/tunnel logs (spike)
/.logs/

# local lint report
.lint.json

# local dev tooling (portable binaries, e.g. cloudflared for the trigger tunnel)
/.tools/
